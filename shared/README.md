# Shared

This directory contains cross-boundary contracts and generated types.

Guidelines:

- Keep protocol definitions in `contracts/`.
- Keep generated frontend types in `types/`.
- Avoid runtime business logic here.
