# ============================================
# Life Records - 前端环境变量示例（本文件不会被加载）
# 使用：复制为 .env.local，Vite 会自动加载 .env.local
# 加载位置：app/ 目录（vite.config 的 root）
# ============================================

# 后端 API 根地址（无尾部斜杠）
# 不配置则无法登录与云端同步，仅本地使用
# VITE_API_URL=http://localhost:8080
