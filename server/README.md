# Server

This directory contains backend services for the project.

Planned structure:

- `cmd/api`: API entrypoint
- `internal/`: domain and infrastructure modules
- `migrations/`: database migrations
