PORT=8080
JWT_SECRET=change-me-in-production
# Optional: enable PostgreSQL persistence (run migrations first: make migrate-up)
# DATABASE_URL=postgres://user:pass@localhost:5432/dbname?sslmode=disable

# Optional: store refresh tokens in Redis (otherwise use Postgres sessions or in-memory)
# REDIS_URL=redis://localhost:6379/0

# Optional: CORS allowed origins (comma-separated; "*" = allow all; empty = no CORS)
# CORS_ORIGINS=http://localhost:5173,https://app.example.com

# Optional: auth rate limit (requests per minute per IP for /auth/login, /auth/register; 0 = disabled)
# RATE_LIMIT_AUTH=60
