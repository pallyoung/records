# ============================================
# Life Records - 后端环境变量示例
# 复制为 .env 后按需修改。在 server 目录下启动时，main 会通过 godotenv 自动加载本目录的 .env（可选，文件不存在不报错）
# ============================================

# 必填：服务端口与 JWT 密钥
PORT=8080
JWT_SECRET=change-me-in-production

# --------------------------------------------
# 数据库与缓存（可选）
# --------------------------------------------
# PostgreSQL：配置后启用持久化，首次需执行 make migrate-up
# DATABASE_URL=postgres://user:pass@localhost:5432/dbname?sslmode=disable

# Redis：配置后刷新令牌存 Redis；不配则用 Postgres 或内存
# REDIS_URL=redis://localhost:6379/0

# --------------------------------------------
# 网络与限流（可选）
# --------------------------------------------
# CORS：逗号分隔来源；* 表示允许全部；空则不启用
# CORS_ORIGINS=http://localhost:5173,https://app.example.com

# 登录/注册：每 IP 每分钟请求上限，0 表示不限制
# RATE_LIMIT_AUTH=60

# --------------------------------------------
# 阿里云 OSS（可选，三者齐配启用真实签名，否则返回 mock URL）
# --------------------------------------------
# OSS_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com
# OSS_ACCESS_KEY_ID=your-access-key-id
# OSS_ACCESS_KEY_SECRET=your-access-key-secret
# OSS_BUCKET=your-bucket-name

# --------------------------------------------
# OpenAI 兼容 AI 接口（可选，两者齐配启用真实 AI，否则 mock）
# --------------------------------------------
# AI_API_URL=https://api.openai.com
# AI_API_KEY=your-api-key
# AI_DEFAULT_MODEL=gpt-3.5-turbo
