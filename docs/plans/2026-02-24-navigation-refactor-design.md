# 导航重构设计方案

**日期**: 2026-02-24
**主题**: 优化App导航结构，解决头部拥挤和习惯功能层级过深的问题

## 问题背景

1. **头部拥挤**：设置、习惯、标签、复盘四个入口都放在头部，空间拥挤且不利于扩展
2. **习惯层级过深**：习惯与记录应该是平级关系，但当前只能通过点击进入才能看到，不利于使用

## 设计目标

- 解耦头部功能，提升扩展性
- 习惯与事务（记录）平级显示，访问便捷
- 整体导航结构清晰，符合用户心智模型

---

## 整体结构

### 底部标签栏（固定，3个标签）

```
┌────────────┬────────────┬───────────┐
│   习惯     │   事务     │  个人中心  │
└────────────┴────────────┴───────────┘
```

**说明**：
- 移除原有的顶部 header
- 习惯和事务平级显示，通过底部标签切换
- 辅助功能（复盘、设置、标签）收纳到个人中心

---

## 各标签页设计

### 1. 事务标签页（原记录功能）

```
┌─────────────────────────────────────┐
│  今日待完成: 3    今日延期: 1       │  ← Dashboard统计（可点击）
└─────────────────────────────────────┘
├─────────────────────────────────────┤
│  FilterBar (搜索和过滤)               │
├─────────────────────────────────────┤
│  Timeline (记录列表)                  │
│                                     │
└─────────────────────────────────────┘
```

**交互**：
- Dashboard 统计区域位于顶部
- 点击统计区域 → 跳转 DashboardDetailPage（二级页面，非drawer）
- 其他交互保持不变

### 2. 习惯标签页

直接显示现有的 HabitsPage，无变化

### 3. 个人中心标签页

```
┌─────────────────────────────────────┐
│          个人中心                   │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│  📊  复盘           >               │
├─────────────────────────────────────┤
│  ⚙️  设置           >               │
├─────────────────────────────────────┤
│  🏷️  标签管理        >               │
└─────────────────────────────────────┘
```

**交互**：
- 列表卡片样式
- 点击各项 → 进入对应页面

---

## 页面层级

```
App
├── TabBar (底部固定)
│   ├── 习惯 → HabitsPage
│   ├── 事务 → RecordsPage
│   │   ├── Dashboard统计 (顶部可点击)
│   │   ├── FilterBar
│   │   └── Timeline
│   └── 个人中心 → ProfileCenterPage (新建)
│       └── 点击进入 → 复盘/设置/标签管理页
└── 子页面（全屏覆盖原有内容）
    ├── DashboardDetailPage
    ├── ReviewPage
    ├── SettingsPage
    └── TagManagementPage
```

---

## 命名变更

| 原名称 | 新名称 | 说明 |
|--------|--------|------|
| 记录 | 事务 | 更准确反映功能含义 |

---

## 技术要点

### 状态管理

**变更前**：
```tsx
const [showReview, setShowReview] = useState(false);
const [showTagManagement, setShowTagManagement] = useState(false);
const [showHabits, setShowHabits] = useState(false);
const [showSettings, setShowSettings] = useState(false);
```

**变更后**：
```tsx
// 底部标签状态
const [activeTab, setActiveTab] = useState<'habits' | 'records' | 'profile'>('records');

// 子页面状态（保持不变）
const [showDashboardDetail, setShowDashboardDetail] = useState(false);
const [showReview, setShowReview] = useState(false);
const [showTagManagement, setShowTagManagement] = useState(false);
const [showSettings, setShowSettings] = useState(false);
```

### 新增组件

- **TabBar** - 底部标签栏组件
- **ProfileCenterPage** - 个人中心页面组件

### 页面渲染逻辑

1. 优先渲染子页面（全屏覆盖）
2. 根据 `activeTab` 渲染对应标签页内容

### 样式调整

- 移除 `.app-header` 样式
- 添加底部 TabBar 固定定位样式
- Dashboard 从 drawer 样式调整为顶部统计栏样式
- ProfileCenterPage 列表卡片样式

---

## 涉及文件

| 文件路径 | 变更类型 |
|----------|----------|
| `src/App.tsx` | 重构导航逻辑 |
| `src/components/dashboard/` | 调整为顶部统计栏样式 |
| `src/components/dashboard-detail/` | 保持不变（页面跳转） |
| `src/components/tab-bar/` | 新建 |
| `src/pages/profile-center-page/` | 新建 |
| `src/App.css` | 移除 header 样式，添加 TabBar 样式 |

---

## 向后兼容

- 数据模型无变化
- 现有页面组件逻辑保持不变
- 仅调整导航入口和状态管理方式
